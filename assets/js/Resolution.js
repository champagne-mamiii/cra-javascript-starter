const { default: Resolution } = require('@unstoppabledomains/resolution');
const resolution = new Resolution();
resolution
multiChainAddr('udtestdev-usdt.crypto', 'USDT', 'ERC20')

then((receiverUSDTAddress) => {
on Ethereum(ERC20 version)
})catch(console.error);